{
  "name": "fake-shopping",
  "version": "1.0.0",
  "description": "This is fake-shopping Application",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx live-server ./dist -p 1234",
    "dev": "parcel serve ./src/index.html",
    "build": "rimraf dist && parcel build ./src/index.html"
  },
  "author": "Preetham",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "parcel": "^2.12.0",
    "rimraf": "^6.0.1"
  }
}
